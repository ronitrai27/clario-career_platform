import React from 'react'

const InterviewStart = () => {
  return (
    <div>
       {/* <div className="flex flex-col items-center justify-center w-[600px] h-[600px] gap-4">
        <video
          ref={videoRef}
          autoPlay
          playsInline
          muted
          className="w-full h-full object-cover rounded-xl"
        />

        <div className="absolute bottom-8 flex gap-4">
          <button
            onClick={startCamera}
            className="px-5 py-2 bg-green-500 hover:bg-green-600 text-white rounded-md font-medium"
          >
            Start Camera
          </button>
          <button
            onClick={stopCamera}
            className="px-5 py-2 bg-red-500 hover:bg-red-600 text-white rounded-md font-medium"
          >
            Stop Camera
          </button>
        </div>
      </div> */}
    </div>
  )
}

export default InterviewStart
